<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DoraCloset - Products</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        body {
            background-color: #fde8e8; /* light pink background */
            font-family: 'Arial', sans-serif; 
        }
        h2 {
            color: black; /* black header text */
        }
        .product-card {
            border: 1px solid #ccc;
            border-radius: 10px;
            padding: 20px;
            margin: 10px;
            text-align: center;
            background-color: #fff;
            transition: transform 0.3s ease-in-out;
        }
        .product-card img {
            max-width: 100%;
            max-height: 150px;
            margin-bottom: 15px;
            border-radius: 5px;
        }
        .product-card:hover {
            transform: translateY(-5px); /* Lift card slightly on hover */
            box-shadow: 0 4px 8px rgba(0,0,0,0.1); /* Add subtle shadow */
        }
        footer {
            position: fixed;
            bottom: 0;
            width: 100%;
            text-align: center;
            padding: 10px 0;
            font-size: 14px;
            color: #666; /* grey text color */
        }
    </style>
</head>
<body>
    <%- include('navbar') %> 

    <div class="container mt-4">
        <div class="row">
            <% products.forEach(product => { %>
                <div class="col-md-4">
                    <div class="product-card">
                        <img src="/images/<%= product.image %>" alt="<%= product.productName %>">
                        <h5><%= product.productName %></h5>
                        <p>Price: $<%= product.price %></p>
                        <p>Quantity: <%= product.quantity %></p>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <footer>&copy; 2024 DoraCloset. All rights reserved. | Contact us at <a href="mailto:contact@doracloset.com">contact@doracloset.com</a></footer>

</body>
</html>
